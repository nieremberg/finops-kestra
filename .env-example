# Configurações do PostgreSQL
POSTGRES_USER=kestra
POSTGRES_PASSWORD=kestra
POSTGRES_DB=kestra

# Configurações do Apache Superset
SUPERSET_USER=admin
SUPERSET_PASSWORD=admin

# Configurações do Kestra
KESTRA_ADMIN_USER=admin
KESTRA_ADMIN_PASSWORD=admin

# Configurações do Apache Pivot
PIVOT_USER=admin
PIVOT_PASSWORD=admin

# Configurações do Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Configurações de Portas (opcional, valores padrão já configurados no docker-compose)
# POSTGRES_PORT=5432
# KESTRA_PORT=8080
# SUPERSET_PORT=8088
# PIVOT_PORT=9090
# NGINX_PORT=80
# PROMETHEUS_PORT=9090
# GRAFANA_PORT=3000
# NODE_EXPORTER_PORT=9100

# Configurações de Recursos (opcional)
# POSTGRES_MEMORY_LIMIT=1G
# KESTRA_MEMORY_LIMIT=2G
# SUPERSET_MEMORY_LIMIT=2G
# PIVOT_MEMORY_LIMIT=1G
# NGINX_MEMORY_LIMIT=512M
# PROMETHEUS_MEMORY_LIMIT=1G
# GRAFANA_MEMORY_LIMIT=1G
# NODE_EXPORTER_MEMORY_LIMIT=256M

# Configurações de Backup
BACKUP_RETENTION_DAYS=7
BACKUP_DIR=./backups

# Configurações de Monitoramento
PROMETHEUS_RETENTION_TIME=15d
GRAFANA_SESSION_TIMEOUT=24h
